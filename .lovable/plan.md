

## 创建管理员账号

通过已部署的 `create-admin` 后台函数为你创建第一个管理员账号。

### 操作内容

调用 `create-admin` Edge Function，传入：
- 邮箱：`87113911@qq.com`
- 密码：`pzy5565283`

该函数会：
1. 检查当前是否已有管理员（如已存在会拒绝创建）
2. 创建 Supabase Auth 用户并自动确认邮箱
3. 在 `user_roles` 表中写入 `admin` 角色

### 后续

创建成功后，你可以直接到 `/admin/login` 用这组账号密码登录。

### 安全提醒

你刚刚把账号密码发到了对话里。建议登录后立刻在后台修改密码（或者告诉我，我可以帮你换一个新密码），避免长期使用这个已被记录的密码。

